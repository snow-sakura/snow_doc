# 声音克隆网站 — 研发需求与分步执行计划

> **来源**：基于课件《【05集】Claude Code + Cursor 实战：从零搭建声音克隆网站，自动写代码+自动调试上线！》提炼并工程化。  
> **用途**：将本文档整体交给 Cursor / Claude，按 **Phase 顺序** 一次只执行一个阶段；每阶段结束后再开启下一阶段对话（或 `/clear` 清空上下文后携带本文件与仓库状态继续）。  
> **范围**：个人本地使用、轻量部署；核心能力为 **文本转语音（TTS）** 与 **声音克隆相关能力**（通过 **Fish Audio** 服务端 API 实现，具体端点以官方文档为准）。
---

## 1. 文档说明

| 项 | 说明 |
|----|------|
| 执行单位 | 每个 **Phase** 视为一个独立「计划」；执行时声明：`仅执行 Phase N，不要提前实现后续阶段。` |
| 进度追踪 | 根目录维护 `Todo.md`（或项目约定的 Todo 文件），**每完成一条子任务即勾选**；与 `CLAUDE.md` 中的协作约定保持一致。 |
| 并行策略 | 在单阶段内部，可使用 **Task 子代理** 拆分无依赖子任务并行完成；子代理之间 **边界清晰、避免改同一文件**。 |
| 资料查询 | 通过 **Context7 MCP**（或项目已配置的文档 MCP）拉取 **Fish Audio** 最新 API 说明，并将关键结论写入仓库内 **本地参考文档**（如 `docs/fish-audio-api-notes.md`）。 |

---

## 2. 项目目标与范围

### 2.1 目标

- 搭建一个可在 **本机运行** 的声音克隆主题网站（或 Web 应用）。
- 完成 **依赖安装 → 后端能力 → 前端页面 → 自检与总结 → 配置运行与上线（本地）** 的闭环。
- 流程上可复现课件中的 **多子代理协作、接口文档先行、阶段性存档** 等方法。

### 2.2 非目标（课件明确或隐含）

- 第一版 **不追求** 复杂视觉与动效；界面以 **简约、核心功能可用** 为主，后续可用专业设计工具迭代。
- 不一次性建设复杂账号体系、多租户、高可用集群等（除非后续单独立项）。

### 2.3 核心功能（MVP）

1. **文本转语音**：用户输入文本，调用 Fish Audio（或选定模型路线）生成音频并返回/下载。  
2. **声音克隆相关能力**：按 Fish Audio 官方能力设计（如创建/选择 voice、参考音频上传等 —— **以 Context7 查到的 API 为准**）。  
3. **本地持久化（轻量）**：使用 **SQLite** 存储必要数据（如任务记录、voice id、配置项等，具体表结构在 Phase 4 定义）。

---

## 3. 约束与假设

- **部署环境**：个人电脑本地；可接受局域网访问配置作为扩展项。  
- **密钥**：Fish Audio **API Key** 仅保存在 **环境变量或本地 env 文件**（如 `.env`），**禁止** 提交到 Git；提供 `.env.example` 说明变量名。  
- **技术栈**：采用 **社区主流、易与 AI 协作** 的组合；数据库固定 **SQLite**。  
- **课件笔误对照**：`White clone` → 实际仓库名以当前目录为准（如 `voice-clone`）；`claud` → `CLAUDE.md`；`to do` → `Todo.md`。

---

## 4. 全局执行约定（交给 AI 时必须遵守）

1. **阶段闸门**：未经用户确认，不跨 Phase 实现功能。  
2. **Todo 同步**：开始 Phase 前把该 Phase 子任务写入 `Todo.md`；完成即勾选。  
3. **子代理**：并行时每个子代理 **单一职责**（例如：仅 API 封装 / 仅 DB 层 / 仅前端表单）。  
4. **规则漂移**：子代理可能忽略仓库规则；主会话负责 **审查 diff 与关键配置**。  
5. **上下文管理**：大阶段切换时：可 `/compact` 压缩对话；在 **已提交 Git** 的前提下，可使用 `/clear` 类指令清空对话上下文，再附本文件与「当前 Phase」继续。  
6. **Fish Audio**：任何请求路径、字段、鉴权方式 **以官方文档 + Context7 查询结果** 为准，避免凭记忆臆造端点。

---

## 5. Phase 0 — 工程初始化与协作文件

**目的**：仓库可打开、规则可见、任务可追踪。

**输入**：空仓库或现有空项目目录。

**执行步骤**

1. 确认使用 Cursor 打开项目根目录。  
2. 创建/校验 `CLAUDE.md`：包含「先更新 Todo」「可使用 Task 并行」等约定（与课件一致即可）。  
3. 创建/校验 `Todo.md`：写入后续 Phase 的 **占位任务列表**（可先写标题，实施时再细化）。  
4. 建立 `README.md`（极简）：项目名、本地运行前置条件、环境变量说明入口。  
5. 建立 `.gitignore`：包含 `.env`、`__pycache__`/`node_modules`/构建产物等（按所选栈补充）。

**验收标准**

- [ ] 根目录存在 `CLAUDE.md`、`Todo.md`。  
- [ ] `.env` 已被忽略；存在 `.env.example`（可在 Phase 8 再补全键名，但本阶段至少预留说明位）。

**交给 AI 的提示模板**

```text
仅执行 Phase 0（工程初始化与协作文件）。不要实现业务代码。
完成后更新 Todo.md 勾选情况，并列出已创建/修改的文件清单。
```

---

## 6. Phase 1 — Fish Audio 资料与本地 API 笔记

**目的**：接口事实来源落地到仓库，减少后续实现偏差。

**执行步骤**

1. 在 Cursor 中确认 **Context7 MCP**（或等价文档 MCP）已连接（课件：通过 `/mcp` 或当前 IDE 的 MCP 面板自检）。  
2. 查询 **Fish Audio** 官方文档中与下列主题相关章节：鉴权、速率限制、TTS、Voice/Clone 相关端点、错误码、示例请求。  
3. 在仓库创建 `docs/fish-audio-api-notes.md`，记录：  
   - 基地址与环境（如 test/prod 若有区分）  
   - 鉴权头格式  
   - MVP 将用到的端点列表与请求/响应要点  
   - 官方文档 URL（完整可复制）  
4. 若课件建议「保存到本地」：可将关键片段以 **链接 + 摘要** 形式写入上述 md（避免整篇侵权式全文粘贴，以摘要为主）。

**验收标准**

- [ ] `docs/fish-audio-api-notes.md` 存在且含 **可操作的** 接口信息（不仅口号）。  
- [ ] 文档内注明 **最后同步日期** 与查询来源（Context7 / 官网路径）。

**交给 AI 的提示模板**

```text
仅执行 Phase 1。使用 Context7 MCP 查询 Fish Audio 官方文档，生成 docs/fish-audio-api-notes.md。
不得实现后端业务代码；仅文档与必要的目录结构。
```

---

## 7. Phase 2 — 需求复述与技术栈评审

**目的**：把「个人本地 + SQLite + Fish Audio + 简约前端」写成一页纸决策，避免返工。

**执行步骤**

1. 输出 `docs/tech-stack-decision.md`，包含：  
   - 前后端框架选型（二选一或组合需 **写明理由**：生态、部署、与 SQLite 集成难度）  
   - 与 Fish Audio 的集成方式（服务端代理 vs 前端直连 —— 默认推荐 **服务端代理** 以保护 API Key）  
   - 风险列表（密钥泄露、音频上传大小、超时、并发）  
2. 与用户（或产品方）对齐：若需调整栈，在本文档顶部 **变更记录** 区登记。

**验收标准**

- [ ] 技术栈 **明确到具体框架名称**（例如 Python+FastAPI / Node+Express 等，择一）。  
- [ ] 明确 API Key 存放位置与 **绝不进 Git** 的策略。

**交给 AI 的提示模板**

```text
仅执行 Phase 2。阅读 docs/fish-audio-api-notes.md 与课件范围，撰写 docs/tech-stack-decision.md。
给出单一推荐方案；如有备选，说明取舍。不要写实现代码。
```

---

## 8. Phase 3 — 依赖安装与最小可运行脚手架

**目的**：本地可启动「空壳」服务（健康检查 / 静态页二选一）。

**执行步骤**

1. 按 Phase 2 选定栈初始化项目结构（`src/` 或约定目录）。  
2. 锁定依赖版本（`requirements.txt` 或 `package.json` 等）。  
3. 提供最小启动命令（写入 `README.md`）。  
4. 增加 `/health` 或等价探活路由（便于后续调试）。

**验收标准**

- [ ] 一条命令可启动开发服务且无报错。  
- [ ] `/health`（或等价）返回 200。

**交给 AI 的提示模板**

```text
仅执行 Phase 3。按 docs/tech-stack-decision.md 落地最小脚手架与依赖文件。
Fish Audio 仅预留模块边界（如 services/fish_audio_client.py），不要实现完整业务。
```

---

## 9. Phase 4 — 后端：SQLite + Fish Audio 核心业务

**目的**：MVP 后端能力可用（先后端、后前端，符合课件顺序）。

**执行步骤**

1. 设计 SQLite schema（迁移脚本或 ORM 模型），至少覆盖：  
   - 合成任务记录（输入文本、状态、错误信息、生成文件路径或对象存储键）  
   - Voice 相关标识（以 Fish Audio 返回字段为准）  
2. 实现 **Fish Audio HTTP 客户端**（超时、重试策略、错误映射为业务错误码）。  
3. 实现 API：  
   - `POST` 文本转语音  
   - 与克隆相关的接口（严格对照 `docs/fish-audio-api-notes.md`）  
4. 单元测试或最小集成测试（可用 mock，避免每次扣费；真实联调留到 Phase 8）。

**验收标准**

- [ ] 无 API Key 时服务 **明确失败**（提示配置 `.env`）。  
- [ ] 核心路由有基本校验（空文本、过长文本、文件类型等）。  
- [ ] SQLite 文件路径可配置，默认在仓库外或 `data/` 并已被 `.gitignore`。

**交给 AI 的提示模板**

```text
仅执行 Phase 4。实现后端与 SQLite；所有 Fish Audio 调用必须与 docs/fish-audio-api-notes.md 一致。
并行时拆分子代理：DB 模型 / HTTP 客户端 / 路由层 分工，避免冲突。
```

---

## 10. Phase 5 — 前端：简约 UI

**目的**：用户可在浏览器完成 MVP 操作。

**执行步骤**

1. 按 Phase 2 栈选择前端形态（模板引擎 / SPA）。  
2. 页面最小集：  
   - 文本输入 + 提交合成  
   - 播放或下载返回音频  
   - 克隆相关表单（若 API 需要参考音频：提供上传与进度提示）  
3. 所有密钥仅走后端；前端只调用本站 API。

**验收标准**

- [ ] 主流程在本地可走通（与 Phase 8 联调衔接）。  
- [ ] 基础表单校验与错误提示（网络错误、422、500 友好文案）。

**交给 AI 的提示模板**

```text
仅执行 Phase 5。只做最小 UI，不引入重型组件库除非已有决策。
与 Phase 4 路由契约保持一致；如需改契约，先更新 docs/ 下说明再改代码。
```

---

## 11. Phase 6 — 代码审查、项目总结与测试准备

**目的**：课件强调「不要立刻跑起来就完事」；先做交叉检查与总结。

**执行步骤**

1. 组织 **至少两轮** 检查（可用子代理分工）：安全（密钥、路径遍历）、依赖漏洞常识检查、错误处理完整性。  
2. 输出 `docs/project-review.md`：已知问题清单（分 P0/P1/P2）、测试用例列表、后续优化建议。  
3. 对 P0 问题 **必须修复** 后再进入 Phase 8 联调。

**验收标准**

- [ ] `docs/project-review.md` 存在且含 **可执行测试步骤**（手工即可）。  
- [ ] P0 为空或已全部解决。

**交给 AI 的提示模板**

```text
仅执行 Phase 6。不要新增业务功能。子代理交叉审查后输出 docs/project-review.md，
并仅修复 P0 问题；P1/P2 记入文档待办。
```

---

## 12. Phase 7 — 版本存档与上下文切换

**目的**：阶段点可回滚，便于长对话环境下继续开发。

**执行步骤**

1. 关闭无关编辑器缓冲（减少误改）。  
2. `git status` 确认变更；**提交**当前可运行版本（提交信息写清阶段：如 `chore: checkpoint after phase 6`）。  
3. 在对话中使用 `/compact` 或项目支持的压缩指令（可选）。  
4. 大阶段切换：在已提交前提下使用 `/clear` 或新开对话，附带：  
   - 本文件路径  
   - 当前 `Todo.md`  
   - 下一 Phase 编号

**验收标准**

- [ ] Git 存在对应 checkpoint 提交。  
- [ ] `Todo.md` 与实现进度一致。

**交给 AI 的提示模板**

```text
仅执行 Phase 7。只做 Git 提交与文档中的变更记录（如有），不写新功能。
```

---

## 13. Phase 8 — 环境变量、本地运行与「上线」

**目的**：课件中的「启动前配置 API Key」与本地可访问。

**执行步骤**

1. 在 `.env.example` 中列出全部变量（含 `FISH_AUDIO_API_KEY` 或实际官方命名）。  
2. 在 `README.md` 写明：复制 `.env.example` → `.env` → 填入密钥。  
3. 本地全链路联调（真实调用 Fish Audio），记录一次成功请求与响应时间。  
4. 「上线」定义为本机可访问 +（可选）同局域网设备访问说明；若需公网部署，另开附录评估（反向代理、HTTPS、限流）。

**验收标准**

- [ ] 按 README 从零克隆仓库可在本地跑通 MVP。  
- [ ] `.env` 未进入版本库；密钥不出现在日志中。

**交给 AI 的提示模板**

```text
仅执行 Phase 8。完善 README 与 .env.example；协助我完成一次真实联调并记录结果到 docs/runbook-local.md。
```

---

## 14. 附录 A — 总览：交给 AI 的「主控 Prompt」（从 0 到 1）

```text
你是本仓库的实现代理。严格按「研发需求-声音克隆网站-分步执行计划.md」分 Phase 执行。
规则：
1) 每次只执行一个 Phase，开始前把该 Phase 子任务写入 Todo.md。
2) Fish Audio 以 docs/fish-audio-api-notes.md 为准；未生成前不得臆造端点。
3) 子任务可并行时使用 Task 子代理，但避免多人改同一文件。
4) 每个 Phase 结束输出：变更文件列表、如何验证、Todo.md 更新片段。
当前授权执行：Phase ___（由用户填写编号）。
```

---

## 15. 附录 B — 课件方法映射（可追溯）

| 课件要点 | 本文档落点 |
|----------|------------|
| 创建项目、Cursor、终端、唤醒 Claude | Phase 0 + 全局约定 |
| `CLAUDE.md` + `Todo.md` | Phase 0 |
| Context7 查 Fish Audio、保存本地 | Phase 1 |
| SQLite、主流栈、反向验证栈 | Phase 2 |
| 先 MVP、先后端后前端、分阶段计划 | Phase 4 → Phase 5 |
| Task 并行、子代理监控 | Phase 4/5/6 说明 |
| 先深度检查再测试 | Phase 6 |
| `/compact`、阶段后存档提交 | Phase 7 |
| 配置 API Key 启动 | Phase 8 |

---

## 16. 附录 C — 风险与对策（执行中随时补充）

- **API 变更**：以 Context7 定期 diff；在 `docs/fish-audio-api-notes.md` 维护变更记录。  
- **费用与配额**：联调与压测前在文档中写明限额策略。  
- **音频与隐私**：本地存储路径、清理策略、用户提示（若未来有多用户需升级）。  

---

**文档版本**：v1.0（由课件全文抽取 + 工程化补全）  
**建议存放路径**：与课件同级目录（本仓库根目录），文件名即本文件。
