# TestHub 快速开始指南

## 🚀 5 分钟快速上手

### 第一步：环境准备

```bash
# 1. 克隆项目
git clone <repository-url>
cd testhub_platform

# 2. 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 3. 安装依赖
pip install -r requirements.txt
```

### 第二步：配置环境

```bash
# 1. 复制环境配置
cp .env.example .env

# 2. 编辑 .env（至少配置以下项）
cat > .env << EOF
SECRET_KEY=your-secret-key-here
DEBUG=True
DB_NAME=testhub
DB_USER=root
DB_PASSWORD=your-mysql-password
DB_HOST=127.0.0.1
DB_PORT=3306
REDIS_URL=redis://127.0.0.1:6379/0
EOF
```

### 第三步：初始化数据库

```bash
# 1. 创建数据库（MySQL）
mysql -u root -p
# CREATE DATABASE testhub CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
# EXIT;

# 2. 执行迁移
python manage.py migrate

# 3. 创建管理员账号
python manage.py createsuperuser
# 按提示输入用户名、邮箱、密码
```

### 第四步：启动服务

```bash
# 终端1：后端服务
python manage.py runserver 0.0.0.0:8000

# 终端2：Celery Worker（可选，用于异步任务）
celery -A backend worker -l info

# 终端3：前端（另开终端）
cd frontend
npm install
npm run dev
```

### 第五步：访问系统

```
前端界面: http://localhost:3000
后端API: http://localhost:8000
管理后台: http://localhost:8000/admin
```

---

## 🤖 AI 大模型配置（5分钟）

### 配置 DeepSeek API

1. 访问 [DeepSeek 开放平台](https://platform.deepseek.com/api-keys)
2. 创建 API Key
3. 在前端配置：

```
路径: 配置中心 → AI 模型配置 → 添加配置

配置项:
- 配置名称: DeepSeek-测试用例编写
- 模型类型: DeepSeek
- 角色: 测试用例编写专家
- API Key: sk-xxxxxxxxxxxxxxxxxxxx
- Base URL: https://api.deepseek.com
- 模型名称: deepseek-chat
- 最大Token: 4096
- 温度参数: 0.7
- Top P: 0.9
```

### 配置通义千问 API

1. 访问 [阿里云 DashScope](https://dashscope.console.aliyun.com/apiKey)
2. 创建 API Key
3. 配置：

```
- 配置名称: 通义千问-用例评审
- 模型类型: 通义千问
- 角色: 测试评审专家
- API Key: sk-xxxxxxxxxxxxxxxxxxxx
- Base URL: https://dashscope.aliyuncs.com
- 模型名称: qwen-plus
```

---

## 📝 AI 提示词配置（5分钟）

### 导入默认提示词

1. 访问 `需求分析 → 提示词配置`
2. 点击 `加载默认提示词`
3. 系统会自动创建：
   - 用例编写提示词
   - 用例评审提示词

### 自定义提示词

如果需要自定义，点击 `添加配置`：

```
- 配置名称: 我的编写提示词
- 提示词类型: 用例编写提示词
- 提示词内容: [粘贴优化后的提示词]
- 启用: ✓
```

---

## 🧪 完整使用流程

### 需求分析 → 测试用例生成

```
1. 上传需求文档
   路径: 需求分析 → 文档上传
   支持: PDF, Word, TXT, Markdown

2. AI 分析需求
   点击"分析"按钮
   AI 自动提取业务需求

3. 生成测试用例
   选择 AI 模型和提示词配置
   点击"生成测试用例"
   等待 AI 生成

4. AI 评审用例
   生成完成后，点击"提交评审"
   AI 自动评审并给出建议

5. 导出用例
   支持导出为 Excel、Markdown
```

---

## ⚡ 常用命令

```bash
# 后端相关
python manage.py runserver          # 启动后端
python manage.py migrate            # 执行迁移
python manage.py createsuperuser    # 创建管理员
python manage.py shell              # Django shell

# Celery 相关
celery -A backend worker -l info    # 启动 Worker
celery -A backend beat -l info     # 启动定时任务调度器

# 前端相关
cd frontend
npm install                         # 安装依赖
npm run dev                         # 开发模式
npm run build                       # 生产构建
```

---

## 🔧 常见问题

| 问题 | 解决方案 |
|------|----------|
| `ModuleNotFoundError` | 重新运行 `pip install -r requirements.txt` |
| 数据库连接失败 | 检查 MySQL 服务和 `.env` 配置 |
| 前端无法访问 | 确认 Vite 开发服务器在 3000 端口运行 |
| AI 调用失败 | 检查 API Key 和网络连接 |

---

## 📚 相关文档

- [完整开发指南](./TestHub项目开发指南.md)
- [AI 配置详解](./AI大模型配置与提示词详解.md)
