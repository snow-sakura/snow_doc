### 声音克隆网站 — Claude实现功能开发步骤

#### 一、准备工作

> 1、创建claude.md和todo.md文件

```
在项目根目录下我创建了一个todo文件，每次在开发之前，你都应该先将我们商量好的代办任务添加到这个文件中。每完成一个任务时，记得把对应的任务标记为已完成，这样可以方便我们实时跟踪开发进度。

合理使用Task工具创建多个子代理来提高开发的效率，每个子代理负责一个独立的任务，互不干扰，支持并行开发。
```

> 2、安装context7

```
使用claude中/mcp查询是否安装连接成功了cotext7
未安装则退出claude，powershell终端命令执行：claude mcp add context7 -- npx -y @upstash/context7-mcp@latest
```

#### 二、Claude中提问

> 1、开始

```
使用context7查询Fish Audio的API文档，并保存到本地，然后访问官方文档链接获得更详细更准确的信息，https://docs.fish.audio/developer-guide/getting-started/introduction，https://docs.fish.audio/api-reference/introduction
```

> 2、向claude描述我们的需求

```
我想搭建一个声音克隆的网站
1、技术栈：Next.js+TypeScript+React+Tailwind CSS v4+shadcn/ui+SQLite
2、克隆功能是通过Fish Audio提供的接口实现的
3、关于接口的详细使用方式请优先参考 docs/fish-audio-api.md
4、我主要是想实现声音克隆和文本转语音这两个功能
5、这个网站只有我自己使用，网站是部署在本地电脑上的
接下来我们一步步规划，你先帮我分析一下我选择的技术栈是否合理，你是否有更合适的方式推荐. ultra think
-- claude 注意切换到计划模式 -- shift tab --plan mode
```

> 3、向claude沟通功能

```
好的，那接下来我们一起来规划一下这个网站应该具有哪些功能？
-- 如果给出的功能太复杂，太多，则暂停继续优化
我感觉你设计的太复杂了，你就围绕着声音克隆和文本转语音这两个核心需求来进行设计，其他所有的功能都可以精简掉，另外，网页也尽可能的简约一些，能满足最基本的需求就可以了
-- 继续优化
好的，根据你的设计方案，你优先开发后端功能，然后再设计网站页面，网站页面只需要实现最基本的交互就可以了，后面我会再重新设计和优化UI。在开发之前，你先执行一份详细的待办任务。任务主要分为三块：第一块，安装所有依赖；第二块，开发后端功能；第三块，开发网站页面。
-- 继续
好的，接下来就按照这个规划来进行开发吧，你需要先把这些任务写入到文件里面
-- 继续
好的，你并没有把任务清单精确到小步骤上去，只是大模块列出来了，请把任务列表清单精确到小步骤上去，并写进todo.md中
-- 之前我有在Claude中说可以使用task工具，你没有使用，你可以使用task工具创建多个子代理来提高开发的效率，要保证子代理之间互补干扰，支持并行开发
-- 继续
我看到你的任务列表并没有全部标记为已完成，是什么原因呢？请核对一下是否有部分功能没有开发完
-- 最后
先不着急讲项目启动起来，你先帮我检查一下这个项目的代码是否存在一些潜在的问题，ultra think
-- 修复
检查修复发现的问题
-- 修复完毕
关闭所有文件，然后进行/init 生成写入claude.md文件
-- 存档提交
请把我当前修改的文件全部提交
-- 测试
好的，接下来我们开始测试这个项目，你告诉我应该怎样启动项目
-- 配置
你不需要帮我启动项目，你只需要告诉我怎么手动启动就可以了
-- 最后
好的，我现在已经配置好了环境变量，那接下来除了最后一步启动项目，其他的步骤你都替我完成  密钥：5c72f950b062467ba5f40d94564a8f1e
```

